# --- Asama 1: veri yukleme ve kesif ---
# Bilincli olarak minimal tutuldu. Her asamada gerekeni ekleyecegiz;
# bastan 30 paket kurmak, birbiriyle cakisan surumler demek.

pandas>=2.2
numpy>=1.26

# Postgres baglantisi
SQLAlchemy>=2.0        # Python tarafindan SQL calistirmak icin
psycopg2-binary>=2.9   # Postgres surucusu

# .env dosyasindaki sifreyi koda gomeden okumak icin
python-dotenv>=1.0

# Kesif ve gorsellestirme
jupyter>=1.0
matplotlib>=3.8
seaborn>=0.13

# --- Asama 3: modelleme ---
# DIKKAT - surum ust siniri bilincli:
# scikit-learn 1.8'de check_array'in 'force_all_finite' parametresi kaldirildi
# (yeni adi 'ensure_all_finite'). optbinning 0.20 hala eski adi cagiriyor,
# dolayisiyla sklearn >= 1.8 ile birlikte calismiyor.
# optbinning bu cagriyi guncelleyene kadar ust sinir kalmali.
scikit-learn>=1.5,<1.8   # baseline lojistik regresyon, metrikler, split
xgboost>=2.1          # gradyan artirmali agaclar
shap>=0.46            # model aciklanabilirligi
optbinning>=0.19      # WOE / optimal binning (scorecard icin)

# --- Asama 4: servis ---
fastapi>=0.115        # skorlama API'si
uvicorn[standard]>=0.32   # ASGI sunucusu
pydantic>=2.9         # istek/yanit dogrulama
httpx>=0.27           # API testleri icin istemci
pytest>=8.3           # test kosucusu
